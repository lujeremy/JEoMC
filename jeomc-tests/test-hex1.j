/* test hex code translation is working properly
   do not run&save the window at the end
*/
int main()
{

  jeomcInit();

  drawCircle(0.0, -0.5, 0.4,"#FFFFFF");
  drawCircle(0.0, -0.5, 0.4,"#9E2828");

  return 0;
}
